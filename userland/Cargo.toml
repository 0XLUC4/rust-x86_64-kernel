[workspace]
resolver = "2"
members = [
    "crates/ulib",
    "init",
    "sh",
    "displayd",
]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "s"
strip = true

[profile.dev]
panic = "abort"
